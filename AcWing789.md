# AcWing 789 - 数的范围

### 📌 题目概述
给定一个按照升序排列的长度为 $n$ 的整数数组，以及 $q$ 个查询。对于每个查询，返回一个元素 $k$ 的起始位置和终止位置（位置从 0 开始计数）。如果数组中不存在该元素，则返回 `-1 -1`。

---

### 💡 核心算法思想：现代 C++ STL 迭代器流
本题是一道标准的二分查找边界题。在考场或机试中，为了追求**高容错率**与**极致的码字速度**，推荐使用现代 C++ 标准库（STL）提供的 `lower_bound` 和 `upper_bound`。

* **起始位置**：利用 `lower_bound` 寻找第一个 **大于等于 ($\ge$) $k$** 的元素。
* **终止位置**：利用 `upper_bound` 寻找第一个 **严格大于 ($>$) $k$** 的元素。因为数组是有序且连续存储的，第一个大于 $k$ 的元素**左边那一位**，必定就是最后一个等于 $k$ 的数（即终止位置）。

---

### 🎓 修士考试（修考）面试与笔试核心高频考点复习指南
*　在日本修考（情报理工专攻）的专业面试或笔试简答题中，教授非常注重数据结构与算法的底层实现原理。以下为本题涉及的核心考点深度复盘：

*　**Q1: 代码中使用的 lower_bound 时间复杂度是多少？有什么使用前提？**
*　满分回答：其时间复杂度为 $\mathcal{O}(\log N)$。
底层物理前提：容器内的元素必须是已经排好序的。
容器必须支持随机访问迭代器（Random Access Iterator），
例如 std::vector 或原生数组。如果作用于 std::list（双向链表），其内部无法通过 $\mathcal{O}(1)$ 的指针偏移进行跳转，二分查找的时间复杂度会退化为 $\mathcal{O}(N)$。

*　**Q2: 为什么条件 it1 == a.end() 可以精准代表“整个数组中没有找到目标值”？**
*　满分回答：现代 C++ 的容器区间均采用左闭右开 [begin, end) 的设计哲学。
a.end() 指向的并不是数组的最后一个元素，而是最后一个元素的下一个越界位置（哨兵位置）。
算法行为：当 lower_bound 扫遍整个数组都没有找到满足 $\ge k$ 的元素时（即数组里所有的数都比 $k$ 小），算法的指针会无可奈何地冲过终点线，最终停在 a.end()。
因此，一旦相等，即代表完全越界未匹配。

*　**Q3: it2 - a.begin() 这一步计算下标的底层原理是什么？时间复杂度是多少？**
*　满分回答：std::vector 在内存中是连续存储的，迭代器在底层是对原生指针的封装。
两个指向同一连续内存的迭代器相减，其底层在硬件级别是两个内存地址相减，再除以单个数据类型所占的字节大小（即 sizeof(int)）。
复杂度：因为这是一次直接的指针算术运算，不需要任何循环遍历，所以这一步的时间复杂度是 $\mathcal{O}(1)$，极其高效。

---

### 💻 纯净源代码 (Modern C++)

```cpp
#include <iostream>
#include <vector>
#include <algorithm> // 必须引入该头文件以使用 lower_bound 和 upper_bound

using namespace std;

int main() {
    // 提高输入输出效率
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int n, q;
    cin >> n >> q;
    vector<int> a(n);
    for (int i = 0; i < n; i++) cin >> a[i];

    while (q--) {
        int k;
        cin >> k;
        
        // 1. 找起始位置（第一个 >= k 的数）
        auto it1 = lower_bound(a.begin(), a.end(), k);
        
        // 哨兵检查：如果迭代器戳到了代表虚无的终点线 a.end()，或者指向的值不等于 k，说明没找到
        if (it1 == a.end() || *it1 != k) {
            cout << "-1 -1\n";
        } else {
            // 利用迭代器相减直接计算 0 标下标
            int start = it1 - a.begin();
            
            // 2. 找第一个 > k 的数
            auto it2 = upper_bound(a.begin(), a.end(), k);
            // 第一个大于 k 的数的前一个位置，就是最后一个等于 k 的数
            int end = (it2 - a.begin()) - 1;
            
            cout << start << " " << end << "\n";
        }
    }
    return 0;
}

---

